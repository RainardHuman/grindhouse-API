spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:grindhouse
    username: sa
    password: P@ssw0rd1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
      show-sql: true
